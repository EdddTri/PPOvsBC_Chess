# Core
chess==1.11.2
numpy==2.4.6
gymnasium==1.2.3
matplotlib==3.10.9

# Reinforcement learning (Agent A)
stable-baselines3==2.8.0
sb3-contrib==2.8.0

# Deep learning. The line below installs the CPU build; for an NVIDIA GPU
# install the matching CUDA wheel instead, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
torch==2.11.0
